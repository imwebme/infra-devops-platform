# Helm chart values
tolerations:
  - effect: NoSchedule
    key: CommonAddonsOnly
    value: "true"
    operator: Equal

nodeSelector:
  service: common-addons

priorityClassName: common-addons

webhook:
  tolerations:
    - effect: NoSchedule
      key: CommonAddonsOnly
      value: "true"
      operator: Equal
  nodeSelector:
    service: common-addons
  priorityClassName: common-addons

certController:
  tolerations:
    - effect: NoSchedule
      key: CommonAddonsOnly
      value: "true"
      operator: Equal
  nodeSelector:
    service: common-addons
  priorityClassName: common-addons