apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: grafana-database-user
  namespace: monitoring
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: aws-cluster-secret-store
  target:
    name: grafana-database-user
    creationPolicy: Owner
  data:
  - secretKey: dbpwd
    remoteRef:
      key: demo/prod/db/grafana
      property: database.password
  - secretKey: dbuser
    remoteRef: 
      key: demo/prod/db/grafana 
      property: database.user
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: mongodb-exporter-user
  namespace: monitoring
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: aws-cluster-secret-store
  target:
    name: mongodb-exporter-user
    creationPolicy: Owner
  data:
  - secretKey: password
    remoteRef:
      key: demo/prod/db/mongodb-exporter
      property: password
  - secretKey: username
    remoteRef:
      key: demo/prod/db/mongodb-exporter 
      property: username
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: mysql-exporter-config
  namespace: monitoring
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: aws-cluster-secret-store
  target:
    name: mysql-exporter-config
    creationPolicy: Owner
  data:
  - secretKey: my.cnf
    remoteRef:
      key: demo/prod/db/mysql-exporter
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: postgres-exporter-config
  namespace: monitoring
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: aws-cluster-secret-store
  target:
    name: postgres-exporter-config
    creationPolicy: Owner
  data:
  - secretKey: postgres_exporter.yml
    remoteRef:
      key: demo/prod/db/postgres-exporter
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: security-prod-db-admin-secret
spec:
  refreshInterval: "1h"
  secretStoreRef:
    name: aws-cluster-secret-store
    kind: ClusterSecretStore
  target:
    name: security-prod-db-admin-secret
    creationPolicy: Owner
  data:
    - secretKey: example-org_admin
      remoteRef:
        key: demo/prod/db/admin
        property: example-org_admin