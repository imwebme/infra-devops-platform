---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: devops-workloads-helm-external-appset
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=invalid"]
  syncPolicy:
    preserveResourcesOnDeletion: true
  generators:
    - matrix:
        generators:
          - matrix:
              generators:
                - git:
                    repoURL: https://github.com/wetripod/devops-gitops-manifest.git
                    revision: main
                    files:
                      - path: configs/**/workloads/helm-external-elements.yaml
                - list:
                    elementsYaml: "{{ .key.components | toJson }}"
          - clusters:
              selector:
                matchLabels:
                  aws_cluster_name: "{{ index .path.segments 1 }}"
  template:
    metadata:
      name: "devops-{{ .metadata.labels.aws_cluster_name }}-workload-{{ .chart.name }}"
    spec:
      project: devops-core
      sources:
        - chart: "{{ .chart.name }}"
          repoURL: "{{ .chart.repo }}"
          targetRevision: "{{ .chart.version }}"
          helm:
            releaseName: "{{ .chart.name }}"
            ignoreMissingValueFiles: true
            valueFiles:
              - $values/environments/base/workloads/{{ .chart.values_path }}
              - $values/environments/{{ .metadata.labels.environment }}/workloads/{{ .chart.values_path }}
              - $values/clusters/{{ .metadata.labels.aws_cluster_name }}/workloads/{{ .chart.values_path }}
        - repoURL: https://github.com/wetripod/devops-gitops-manifest.git
          targetRevision: main
          ref: values
      destination:
        namespace: "{{ .chart.namespace }}"
        name: "{{ .name }}"
      syncPolicy:
        syncOptions:
          - CreateNamespace=true
          - Validate=false
          - PrunePropagationPolicy=foreground
          - PruneLast=false
          - ApplyOutOfSyncOnly=true
          - ServerSideApply=true
      ignoreDifferences:
        - kind: Secret
          name: aws-load-balancer-tls
          jsonPointers: [/data]
        - group: admissionregistration.k8s.io
          kind: MutatingWebhookConfiguration
          jqPathExpressions: [".webhooks[].clientConfig.caBundle"]
        - group: admissionregistration.k8s.io
          kind: ValidatingWebhookConfiguration
          jqPathExpressions: [".webhooks[].clientConfig.caBundle"]
        - group: apps
          kind: StatefulSet
          jqPathExpressions:
            - '.spec.volumeClaimTemplates[].apiVersion'
            - '.spec.volumeClaimTemplates[].kind'
        - group: apiextensions.k8s.io
          kind: CustomResourceDefinition
          name: flinkdeployments.flink.apache.org
          jsonPointers:
            - /spec/versions/0/additionalPrinterColumns/0/priority
            - /spec/versions/0/additionalPrinterColumns/1/priority
        - group: apiextensions.k8s.io
          kind: CustomResourceDefinition
          name: flinksessionjobs.flink.apache.org
          jsonPointers:
            - /spec/versions/0/additionalPrinterColumns/0/priority
            - /spec/versions/0/additionalPrinterColumns/1/priority
        - group: apiextensions.k8s.io
          kind: CustomResourceDefinition
          name: flinkstatesnapshots.flink.apache.org
          jsonPointers:
            - /spec/versions/0/additionalPrinterColumns/0/priority
            - /spec/versions/0/additionalPrinterColumns/1/priority
            - /spec/versions/0/additionalPrinterColumns/2/priority
  templatePatch: |
    {{- if .chart.auto_sync }}
    spec:
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
    {{- end }}
